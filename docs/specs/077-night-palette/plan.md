# SPEC-077 — Plan

- `tokens.css`: valores de `[data-theme="dark"]`.
- `patterns.css`: fila elegida con `--ui-accent-soft`.
- `order-inbox.tsx`: `SENT` en neutral; estado y subestado en una sola etiqueta.
