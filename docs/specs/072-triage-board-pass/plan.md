# SPEC-072 — Plan

- `/recovery/triage`: pestañas con cifra, enlace a Repartir, estado vacío
  con el siguiente paso, `QueueFilters` con `moreFilters` y
  `visibleExtras={["batch"]}`; `formatCampaignMoment`.
- `RecoveryTriageForm`: «N de M marcados» y atajos en `<details>`.
- Menú: descripción de Campañas para quien revisa.
- `/recovery/board`: `staleCases` con `recoveryDaysUntouched`, columna de
  equipo condicional, «Cómo se calcula» en `<details>`, conversión vacía en
  una línea.
