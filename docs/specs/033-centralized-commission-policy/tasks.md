# Tasks — SPEC-033

- [x] `PerformanceCommissionPolicy` + `getPerformanceCommissionPolicy()` en
      `@repo/validation` (BR-001, BR-003).
- [x] `getPotentialBaseCommissionCents` y `calculateAcceleratorOne` leen de la
      política (BR-002).
- [x] Conciliación sin literales (`get-performance-reconciliation.ts`).
- [x] Mix de ventas sin literales (`performance-dashboard.tsx`).
- [x] Prueba de coherencia política ↔ tarifas (AC-003).
- [x] Grep de literales fuera de la política sin resultados (AC-001).
- [x] Tipos, lint y equivalencia visual verificados (AC-002).
