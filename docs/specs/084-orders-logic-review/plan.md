# SPEC-084 — Plan

- `get-order-inbox.ts`:
  - `TO_MOVE` y `DONE` corregidos.
  - `summaryWhere` para el resumen.
  - `priorPending` con dos conteos y su rango.
- `order-inbox.tsx`: `ordersHref` con `range`; resumen de meses anteriores.
- `order-next-step.tsx`: confirmación al cerrar.
