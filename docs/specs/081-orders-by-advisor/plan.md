# SPEC-081 — Plan

- `get-order-inbox.ts`: `advisorScopeWhere` (el alcance sin el filtro de asesor), cuatro `groupBy` por `agentUserId` y `advisorSummary`.
- `order-inbox.tsx`: `AdvisorSummary`, una tabla con enlaces por cifra.
